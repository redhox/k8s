apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mlflow
  annotations:
    ngnx.ingress.kubernertes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
  - host: test.morgan-coulm.fr
    http:
      paths:
      - path: /mlflow(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: mlflow-service
            port:
              number: 80
