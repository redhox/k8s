apiVersion: apps/v1
kind: Deployment
metadata:
  name: jupyterhub
  labels:
    app: jupyterhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jupyterhub
  template:
    metadata:
      labels:
        app: jupyterhub
    spec:
      containers:
        - name: jupyterhub
          image: coulm/jupyterhub_arm:latest
          ports:
            - containerPort: 8000
          env:
            - name: JUPYTERHUB_BASE_URL
              value: /jupyterhub/
          volumeMounts:
            - name: jupyterhub-data
              mountPath: /srv/jupyterhub
            - name: jupyterhub-config-volume
              mountPath: /srv/jupyterhub/jupyterhub_config.py
              subPath: jupyterhub_config.py
      volumes:
        - name: jupyterhub-data
          persistentVolumeClaim:
            claimName: pvc-jupyterhub-data
        - name: jupyterhub-config-volume
          configMap:
            name: jupyterhub-config
